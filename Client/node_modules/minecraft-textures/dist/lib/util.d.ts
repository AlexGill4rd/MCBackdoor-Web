import { Item } from './types';
interface Updates {
    [id: string]: Partial<Item>;
}
export declare const updatePropertiesOfItems: (updates: Updates | undefined, items: Item[]) => Item[];
export {};
